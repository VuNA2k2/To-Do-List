import 'package:flutter/material.dart';

class FillProfileScreen extends StatelessWidget {
  const FillProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Fill profile"),),
    );
  }
}
